package ch06.kim


/**
 *
 * 연습문제 6-4
 *
 * SOF가 일어나지 않도록 insertTailrec을 작성해보자.
 *
 * 힌트 : 함수의 선언 타입은 아래와 같다.
 *       필요하다면 내부 함수를 별도로 생성하자.
 *
 */

import ch06.kim.Tree.Node;
import ch06.kim.Tree.EmptyTree;

// 못 풀 겠 다 !!
//https://github.com/funfunStory/fp-kotlin-example/blob/master/src/main/kotlin/fp/kotlin/example/chapter06/solution/6-4-treeInsertTailrec.kt
//tailrec fun Tree<Int>.insertTailrec(elem: Int): Tree<Int> = {
//
//}


fun main(args: Array<String>) {
//    val tree1 = EmptyTree.insertTailrec(5)
////    require(tree1 == Node(5, EmptyTree, EmptyTree))
////    val tree2 = tree1.insertTailrec(3)
////    println(tree2)
////    require(tree2 ==
////            Node(5,
////                    Node(3, EmptyTree, EmptyTree),
////                    EmptyTree)
////    )
//    var tree1 = EmptyTree.insert(5)
//    for (i in 1..100000) {
//        val new = tree1.insert(i)
//        tree1 = new;
//    }
//    println(tree1)
}